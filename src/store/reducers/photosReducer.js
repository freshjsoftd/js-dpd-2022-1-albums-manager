const initialState = {
  photos: [],
  isFetching: false,
  error: null
}
const photosReducer = (state= initialState, {type, payload}) => { 
  switch(type) {
    default: return state;
  }
  
 }

 export default photosReducer;