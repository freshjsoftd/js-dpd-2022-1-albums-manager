const initialState = {
  albums: [],
  isFetching: false,
  error: null
}
const albumsReducer = (state= initialState, {type, payload}) => { 
  switch(type) {
    default: return state;
  }
  
 }

 export default albumsReducer;